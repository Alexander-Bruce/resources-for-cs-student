#include <iostream>
#include <algorithm>
 
using namespace std;
 
int main() {
    int times;
    scanf("%d", &times);
 
    for (int i = 0; i < times; i++) {
        int length;
        scanf("%d", &length);
 
        int arr[length];
        for (int j = 0; j < length; j++) {
            scanf("%d", &arr[j]);
        }
 
        nth_element(arr, arr + length / 2, arr + length);
 
        printf("%d\n", arr[length / 2]);
    }
 
    return 0;
}